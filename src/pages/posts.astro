---
import Navigation from "../components/navigation.astro";
import Layout from "../layouts/Layout.astro";
import PostCard from "../components/postCard.astro";
import GoBack from "../components/goBack.astro";
const allPosts = Object.values(
  import.meta.glob("./posts/*.md", { eager: true }),
);
---

<Layout title="Posts | renzofbn">
  <div class="tracker"></div>
  <div class="flex justify-center">
    <div class="container max-w-4xl text-left px-6 md:px-24 mt-6 md:mt-16">
      <GoBack url="/" text="Home" />
      <div class="mt-8 md:mt-12 border-b-2 border-neutral-800 pb-8">
        <h1 class="text-5xl md:text-6xl mt-4 font-extrabold text-white mb-2">
          Posts
        </h1>
        <p class="md:text-lg text-gray-400">
          Este es un texto alineado a la izquierda dentro de una columna
          centrada.
        </p>
      </div>
      <div>
        {
          allPosts.map((post: any) => (
            <PostCard
              url={post.url}
              title={post.frontmatter.title}
              desc={post.frontmatter.description}
              tags={post.frontmatter.tags}
              date={post.frontmatter.pubDate}
            />
          ))
        }
      </div>
    </div>
  </div>
</Layout>
